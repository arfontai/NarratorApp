<Page
    x:Class="SpeechToTextApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpeechToTextApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Button x:Name="StartRecordButton" Content="Start Record" Margin="76,71,0,0" VerticalAlignment="Top" Click="StartRecordButton_Click"/>
        <Button x:Name="StopRecordButton" Content="Stop Record" Margin="208,71,0,0" VerticalAlignment="Top" Click="StopRecordButton_Click"/>
        <ListBox x:Name="MessageList" Margin="76,134,76,601" ItemsSource="{x:Bind Messages}">
            <ListBox.ItemTemplate>
                <DataTemplate x:DataType="local:Message">
                    <StackPanel>
                        <TextBlock Text="{x:Bind SpeakerName, Mode=OneWay}" FontSize="12" FontWeight="Bold"></TextBlock>
                        <TextBlock Text="{x:Bind MessageText, Mode=OneWay}" FontSize="12"></TextBlock>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Page>
